#include <bits/stdc++.h>
using namespace std;

#define int long long
#ifdef LOCAL_PROJECT
#include "local.h"
#endif

void solve(int tc) {
    
}

int32_t main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int tcn; cin >> tcn;
    for (int tc = 1; tc <= tcn; tc++) solve(tc);

    return 0;
}
