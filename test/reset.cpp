#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL_PROJECT
#include "local.h"
#endif

using ll = long long;
using pii = pair<int, int>;

void solve(int tc) {
    
}

int32_t main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int tcn; cin >> tcn;
    for (int tc = 1; tc <= tcn; tc++) solve(tc);

    return 0;
}
